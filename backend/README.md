# Backend Placeholder

The dashboard currently works as a static frontend. This directory is reserved
for an optional API service if dynamic review analysis is added later.

A future backend should expose an analysis endpoint, load model resources on a
suitable CPU/GPU host, and return the same JSON shape used by the static
dashboard data files.
