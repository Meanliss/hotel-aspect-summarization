# URL of the analyze backend (FastAPI service, implemented later).
# Leave unset to run the Explore tab only (static data, no backend needed).
# Example: NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=
