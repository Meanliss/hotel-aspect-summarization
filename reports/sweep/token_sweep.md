# Token-budget sweep — macro ROUGE F1 (split=all), fixed denominator

Abstractive `--max_new_tokens` (M2/M3/M4). Threshold held at the default. Token budget does not drop entities, so coverage stays flat here and the comparison is clean. `*` = current default (192).


## SPACE

_(no cells yet)_

## HASOS

_(no cells yet)_
